# Docker environment configuration
# Copy to .env and update values, then run: docker-compose up --build

# Database Configuration (MySQL must be running separately)
BACKUPS_PATH=/var/lib/mysql/backups
DB_HOST=localhost
DB_PORT=3306
DB_NAME=checkbook_register
DB_USER=checkbook_user
DB_PASSWORD=secure_password_here
